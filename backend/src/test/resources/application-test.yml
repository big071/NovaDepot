spring:
  datasource:
    url: jdbc:mysql://localhost:13306/novadepot?useUnicode=true&characterEncoding=UTF-8&connectionCollation=utf8mb4_0900_ai_ci&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: root
  data:
    redis:
      host: localhost
      port: 6379
      database: 0

app:
  jwt:
    secret: test_only_jwt_secret_32_chars_minimum
    expire-seconds: 7200
  auth:
    login:
      rate-limit:
        enabled: false
  ai:
    provider: rule
    paid-enabled: false

logging:
  level:
    root: info
    com.novadepot.backend: info

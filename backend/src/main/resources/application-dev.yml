spring:
  datasource:
    url: jdbc:mysql://localhost:3306/novadepot?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: root
  data:
    redis:
      host: localhost
      port: 6379
      database: 0

logging:
  level:
    root: info
    com.novadepot.backend: debug

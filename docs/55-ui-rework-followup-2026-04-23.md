# NovaDepot 页面返工（按截图问题回修）- 2026-04-23

## 1. 返工范围
1. 审计中心筛选交互（下拉可选、可输入、可清空）。
2. 登录页去技术化（隐藏 `tenantCode=default` 输入）。
3. Agent Center 参数区可解释性增强（中文标签与用途说明）。
4. 新手引导可见化（全局入口条 + 仪表盘入口卡片）。
5. 前端权限补强（系统用户页/审计页仅管理员角色进入）。

## 2. 关键改动
1. 审计中心：
   - 模块、动作、资源类型改为 `n-select` + `filterable` + `clearable` + `tag`。
   - 默认展示时间范围、业务编号、仅失败筛选。
2. 登录页：
   - 移除租户输入框，仅保留账号/密码。
   - 继续保留中文“演示账号快捷入口”。
3. Agent Center：
   - 任务参数按任务类型映射中文业务标签和用途说明。
   - 参数输入框增加业务化 placeholder。
4. 新手引导：
   - 工作台新增全局“新手入口”提示条。
   - 仪表盘新增“新手引导入口”卡片与一键开始按钮。
5. 角色安全：
   - 路由层限制 `/system/users`、`/system/audit-center` 为管理员角色。

## 3. 运行与发布说明
1. 本轮优先发布了前端静态资源（`dist` 覆盖到前端容器）。
2. 若需要后端代码变更（管理员重置密码后端硬限制）生效，需要网络恢复后重新构建 backend 镜像并重启。

# NovaDepot v1.4 Backlog Completion

> Branch: `develop/v1.4-backlog-completion`
> Scope: one-time backlog completion after v1.4 Ops & Admin Readiness.

## Original Suggestion Checklist

1. Redis cache adoption for stable reference data.
2. Composite database index review.
3. Backend unit-test completion.
4. AiService decomposition check.
5. AiProvider type-safety check.
6. Frontend `any` cleanup check.
7. Pagination and long-list stability review.
8. v1.2.1 release tag check.

## Current Completion Scan

### Completed

- `ReferenceDataCacheService` exists and is used by products, warehouses, locations, and partners.
- Reference-data cache uses cache-aside behavior and falls back to DB loaders when Redis read/write/evict fails.
- Product, warehouse, location, and partner create/update/import or status-change flows evict reference-data cache prefixes.
- No inventory quantity, login state, JWT, API key, password, or user permission-decision cache was found.
- `docs/115-v1.4-slow-query-index-review.md` records index advice and rollback SQL; no DDL was executed in v1.4.
- AI provider routing, DeepSeek failure, no-fallback behavior, CSV import preload, JWT, request size, CORS, login rate limit, and masking tests already exist.
- `AiService` is a facade over chat, stream, conversation, message, and usage-log services.
- `AiProvider.chat` returns the typed `AiProviderResponse`; external response maps are produced through the mapper layer.
- `frontend-vue/src` has zero explicit `any` scan hits.
- The `v1.2.1` tag already exists; no tag was created or moved.

### Partial

- Redis cache behavior existed in production code, but direct unit coverage for cache hit, miss, fallback, and evict behavior was missing.
- Most `selectList` call sites are bounded by detail IDs, import preload sets, existing page contracts, or known small reference data, but AI tool queries still used `.last("limit ...")`.

### Not Completed Before This Act

- `.last("limit ...")` had residual usage in `AiToolExecutor`.
- `ReferenceDataCacheService` lacked direct backend unit tests.

## Actual Completion Work

- Replaced AI read-only tool `.last("limit ...")` calls with MyBatis-Plus `Page(1, limit, false)` queries.
- Preserved AI tool response shape, limit semantics, permissions, and read-only behavior.
- Added `ReferenceDataCacheServiceTest` for cache hit, cache miss/write, Redis failure DB fallback, and prefix evict behavior.
- Updated this backlog completion record and the docs index.

## Already Complete And Not Repeated

- No AiService re-split was performed.
- No AiProvider type-safety rewrite was performed.
- No frontend `any` cleanup was performed.
- No v1.2.1, v1.3, or v1.4 tag operation was performed.
- No production database schema or migration was changed.
- No v2.0, multi-tenant runtime, billing, RAG, vector database, multimodal, or large business feature work was started.

## Deferred Backlog

- Complex `selectList` call sites that are tied to existing list APIs should be reviewed only in a future API-compatible pagination sprint.
- Composite index DDL remains documentation-only until a separate schema-change approval.
- Agent patrol result caching remains deferred because patrol output reflects changing operational state and should not be cached without a stricter consistency review.
- Backend role-management permission guard has E2E coverage; deeper isolated unit tests can be added in a future low-risk test sprint.

## Impact

- Database: no schema, migration, or data change.
- Permissions: no permission code, RBAC rule, route guard, or menu semantics changed.
- Security: no secret value added; no JWT, login-state, API-key, password, or user permission-decision caching introduced.
- Redis consistency: stable reference data is cache-aside only, tenant-scoped by key prefix, and evicted after supported writes/imports.

## Verification Results

Release-plan verification on `develop/v1.4-backlog-completion` passed:

- `docker compose up -d mysql redis backend frontend-vue`
- `./scripts/ops/reset-commercial-baseline.ps1`
- `./scripts/ops/data-quality-check.ps1`
- `cd backend && mvn test` (`43` tests passed)
- `cd frontend-vue && npm run typecheck`
- `cd frontend-vue && npm run build`
- `cd frontend-vue && npm run test:unit` (`10` tests passed)
- `cd frontend-vue && npm run test:e2e` (`28` tests passed)
- `git diff --check`

Safety and quality scans passed for release preparation:

- No `.env`, `node_modules`, `dist`, `target`, `test-results`, or `tmp` paths appeared in `git status`.
- Secret scans found only placeholders, demo/test passwords, masking test values, and field names; no real API key, JWT secret, database password, private key, or access token was identified.
- `.last("limit")`, `frontend-vue/src` explicit `any`, backend `System.out.println`, and frontend production `console.log/debug/info` scans had zero actionable hits.
